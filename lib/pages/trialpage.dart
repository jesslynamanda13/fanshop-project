import 'package:flutter/widgets.dart';

class TrialPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Trial Page"),
    );
  }
}
